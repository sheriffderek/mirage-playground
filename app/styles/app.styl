
$white = #FFFFFF
$color = #25ACC2 // note the = instead of : for stylus variable declaration

$pad = 10px

html
  background: $color
  color: $white

.page-section
  //
  .inner-w
    max-width: 1200px
    margin-right: auto
    margin-left: auto
    padding: $pad

.account-list
  .account
    span
      opacity: .5
