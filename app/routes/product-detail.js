import Route from '@ember/routing/route';
// import { inject as service } from '@ember/service';


export default Route.extend({

  // thinking about using the resizeService here
  // or if the component is best...
  // but I may want to use or not-use a component based on size at this level...

});
